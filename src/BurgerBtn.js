import React from "react";
import {Button, StyleSheet} from 'react-native';

function BurgerBtn() {
  return(
    <Button style={styles.burgerBtn}>

    </Button>
  );
}

const styles = StyleSheet.create({
  burgerBtn: {
    
  },
});

export default BurgerBtn;